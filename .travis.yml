dist: xenial   # required for Python >= 3.7

language: python

python:
  #- "3.5"
  - "3.6"
  #- "3.7"

sudo: required

addons:
  chrome: stable

before_install:

  - "wget https://chromedriver.storage.googleapis.com/77.0.3865.10/chromedriver_linux64.zip -P ~/"
  - "unzip ~/chromedriver_linux64.zip -d ~/"
  - "rm ~/chromedriver_linux64.zip"
  - "sudo mv -f ~/chromedriver /usr/local/share/"
  - "sudo chmod +x /usr/local/share/chromedriver"
  - "sudo ln -s /usr/local/share/chromedriver /usr/local/bin/chromedriver"

install:
    - "pip install pytest"
    - "pip install selenium"
    - "pip install allure-pytest"
    - "pip install allure-python-commons"

script:
   - "whereis google-chrome-stable"
   - "whereis chromedriver"
   - "python tests/e2e_tests/forgot_login/forgot_login_all_fields_empty_test.py"

env:
  #- CODECOV_TOKEN=<token>#IF ONLY YOU HAVE A PRIVATE REPOSITORY

after_success:
  # Generates coverage report, public repo using Travis:
  # Source: https://dev.to/j0nimost/using-codecov-with-travis-ci-pytest-cov-1dfj
  # - codecov